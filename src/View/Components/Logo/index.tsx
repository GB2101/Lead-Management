import React, { FC } from 'react';

import './style.css';
import logo from '../../Assets/logo-EloGroup.png';

const Logo: FC = () => <img className='logo-img' src={logo} alt='Elo Group Logo' />;
export default Logo;
